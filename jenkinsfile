pipeline {
 agent any
 stages{
       stage("Build"){
                       steps{
                               sh 'touch Build_file'
                            }
                     }
       stage("Prod"){
                       steps{
                               sh 'touch Prod_file'
                            }
                    }
      stage("stage"){
                       steps{
                               sh 'touch Prod_file'
                            }
                    }
  
       }
}
